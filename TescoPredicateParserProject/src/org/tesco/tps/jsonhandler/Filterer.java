package org.tesco.tps.jsonhandler;

public class Filterer {
	
	// UserAgent.json.findEvery("(director|assistant\\s*producer): { email: }"
	// Find all Buyer role users who are responsible for Cosmetics
	// ALLOW User.RoleName == "Buyer" && User.AuthorizationCategory == "Cosmetics"
	//
	// And now in JSON queries ....
	// 
	// json.findEvery("");
	// 

}
